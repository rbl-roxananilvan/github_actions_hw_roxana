name: CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "8.0.x"

      - name: Restore
        run: dotnet restore Roxana_tema1/Roxana_tema1.slnx

      - name: Build
        run: dotnet build Roxana_tema1/Roxana_tema1.slnx --no-restore --configuration Release

      - name: Test (with coverage collection)
        run: dotnet test Roxana_tema1/UnitTests/UnitTests.csproj --no-build --configuration Release --collect:"XPlat Code Coverage"
